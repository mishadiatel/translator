import {Grid, Paper, styled} from "@mui/material";
import Box from "@mui/material/Box";
import {auto} from "@popperjs/core";

export const FieldsContainer = styled(Grid)(({theme}) => ({
    backgroundColor: theme.palette.common.white,
    width: '100%',
    borderRadius: '10px'
}))


