import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        common: {
            gray: '#5F6368',
            textColor: '#3c4043',
            lightGray: '#FAFAFA'
        },
        primary: {
            main: '#1967D2',
        },
        secondary: {
            main: '#5F6368',
        }

    },
    typography: {

    },
});

export default theme;